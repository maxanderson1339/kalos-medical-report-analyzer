{
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install && pnpm --filter @repo/ui run build:styles && pnpm --filter @repo/ui run build:components",
  "buildCommand": "pnpm run build",
  "outputDirectory": ".next"
}
